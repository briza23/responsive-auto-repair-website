import React from "react"; // eslint-disable-line no-unused-vars

const Footer = (type) => {
   const content = (
    <div>
        <p>Footer</p>
    </div>
   );
   return content;
};

export default Footer;