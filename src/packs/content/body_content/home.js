import React from "react"; // eslint-disable-line no-unused-vars

const home = (type) => {
   const content = (
    <div>
        <h1>Home page</h1>
    </div>
   );
   return content;
};

export default home;