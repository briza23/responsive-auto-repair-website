import React from "react"; // eslint-disable-line no-unused-vars
import TopPage from "./home_content/top_page";
const home = (type) => {
   const content = (
    <div>
        <TopPage />
    </div>
   );
   return content;
};

export default home;