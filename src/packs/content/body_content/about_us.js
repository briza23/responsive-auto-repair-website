import React from "react"; // eslint-disable-line no-unused-vars

const aboutUs = (type) => {
   const content = (
    <div>
        <h1>About Us page</h1>
    </div>
   );
   return content;
};

export default aboutUs;