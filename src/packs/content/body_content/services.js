import React from "react"; // eslint-disable-line no-unused-vars

const services = (type) => {
   const content = (
    <div>
        <h1>services page</h1>
    </div>
   );
   return content;
};

export default services;