import React from "react"; // eslint-disable-line no-unused-vars

const Body = (type) => {
   const content = (
    <div>
        <p>Body</p>
    </div>
   );
   return content;
};

export default Body;